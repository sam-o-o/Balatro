import Phaser from 'phaser'
import { empty, push, top, pop, Stack, is_empty, stack_count } from '../src/lib/stack'
import { sizes, deck, Card, Suit, CardSlot, scene_keys, base_chip_req } from '../src/scenes/common'
import * as functions from '../src/lib/balatro_functions'

money_earned(3);